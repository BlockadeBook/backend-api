services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./app:/backend/app
    environment:
      - DATABASE_URL=http://database-core:8080

volumes:
  database_data:
